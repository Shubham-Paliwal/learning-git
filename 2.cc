#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
typedef pair<int, int> ii;
typedef vector<int> vi;
typedef vector<string> vs;
typedef vector<bool> vb;
typedef vector<ii> vii;
#define fi first
#define se second
#define pb push_back
#define mp make_pair
#define matb(n, m, flag) vector(n, vb(m, flag))
#define mati(n, m, val) vector(n, vi(m, val))
#define fr0(n) for(int i = 0; i < n; i++) 
#define fr1(n) for(int i = 1; i <= n; i++) 
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	cout << "shubham";
	cout << " paliwal";
}